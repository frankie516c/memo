<h3>직접 만들어보기</h3>

문제
```
const 기호 = "■";
for( ; ; )
{
  
}
// 출력 예시 : ■■■■■■■■■■ 
```

답
```
    const 기호 = "■";
    let 값 = "";
    for ( let i = 0 ; i < 10 ; i++ )
    {
       값 = 값 + 기호;
    }
     console.log(값);
```
- `값 = 값 + 기호;`를 `값 += 기호;`로 써도 같음

