지난번꺼 다시 복습

```
const App = () => { 
const [a, setA] = useState("A")
const fn1 = () => console.log("fn1")
const fn2 = () => console.log("fn2")
if ( a === "AA" ) console.log("상태값이 변경 되었습니다.")
  const event = () => {
    console.log(1)
    setA("A")
    if(a==="AA") return 
    fn1(); console.log(2)
    fn2(); console.log(3)
  }
  return (
    <>
    <button type='button' onClick={Event}>호출</button>
      <p>{a}</p>
      <p>{b}</p>
      <p>{c}</p>
    </>
  )
}
```

이걸 처음 실행 시키면 콘솔에
```
1
fn1
2
fn2
3
```
로 출력이 되고 (if는 무시되니까)

두 번째 event를 실행시키면 
```
1
```

얘만 출력됨
- 왜? if가 실행되고 아직 a는 "AA"가 아니기 때문에
  - if ( a === "AA" ) console.log("상태값이 변경 되었습니다.") 얘가 실행되지 않음
