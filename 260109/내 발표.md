List.jsx
```
import { useNavigate } from 'react-router'

const List = () => {
  const navigate = useNavigate()
  const onclick = (data) => {
    navigate('detail', {state:data})
  }

const arr = [
    {"key": 1, "name":"스티븐", "email":"jobs@shellfolder.com", "regDate":"2023-02-28", "pwd":"1", "gender":true},
    {"key": 1, "name":"에브릴", "email":"lavigne@shellfolder.com", "regDate":"2023-02-27", "pwd":"2", "gender":false},
  ]

 const styles = {
    "cursor":"pointer"
  }

  return (   
	<div className="container mt-3">
      <h1 className="text-center">목록 화면</h1>
	  <div className="btn-group">
	    <a href="Create.html" className="btn btn-primary">사용자 추가</a>
	  </div>
	  <table className="table table-hover mt-3">
	    <thead className="table-dark">
	      <tr>
	      	<th>이름</th>
	        <th>이메일</th>
	        <th>가입날짜</th>
	      </tr>
	    </thead>
	    <tbody>
        {
          arr.map((v,i) => {
        return (
       <tr style={styles} onClick={()=>onclick(v)} key={i}>
        
          <td>{v.name}</td>
          <td>{v.email}</td>
          <td>{v.regDate}</td>
       
       </tr>
        )
        })
       }
     </tbody>
     </table>
    </div>
  )
    }
    export default List
```

[1]================================
html에서 jsx로 사용할 수 있게 변경하겠습니다
->html 복붙 & className 바꾸기

[2]================================
뿌려줄 데이터를 정리할 배열을 만들겠습니다.
->const arr =[ 이거 채우기

[3]================================
map을 이용해서 목록의 값을 입력하겠습니다.
->tr값 지우고 arr.map 쓰기
->key={i}

[4]================================
onCllick 함수를 사용하여 각각의 목록을 클릭했을때,
상세화면으로 값을 가지고 넘어 갈 수 있도록 하겠습니다. 
-> x

[5]================================
페이지를 넘기기 위해 react-router의 훅인 useNavigate를 사용하겠습니다.
-> const navi
-> onClick
onClick이라는 이벤트가 발생했을 때 data를 갖고 detail화면으로 넘어가기 위해
다음과 같이 작성합니다
-> navigate('
-> <tr에 onClick써주기
상세화면으로 넘어갑니다

[6]================================
목록에 커서 갔을 때 손가락 모양으로 보이게 하기 위해 스타일 주겠습니다.
->cursor:pointer
-> tr에 추가
